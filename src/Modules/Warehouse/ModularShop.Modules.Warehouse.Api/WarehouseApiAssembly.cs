namespace ModularShop.Modules.Warehouse.Api;

/// <summary>
/// Marker type whose only purpose is to let the host locate this assembly and register it as an
/// ASP.NET Core MVC <c>ApplicationPart</c>, so its controllers are discovered.
/// </summary>
public sealed class WarehouseApiAssembly;
