<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <RootNamespace>ModularShop.Modules.Support.Application</RootNamespace>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\ModularShop.Modules.Support.Domain\ModularShop.Modules.Support.Domain.csproj" />
    <!-- Support depends on the kernel (ICurrentUser + the shared Customer entity), but on NO other
         module — it is a genuinely independent module. -->
    <ProjectReference Include="..\..\..\Kernel\ModularShop.Kernel.Application\ModularShop.Kernel.Application.csproj" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="Ardalis.Result" />
    <!-- No EF Core: use cases depend on the repository abstractions + IUnitOfWork, not the DbContext. -->
    <PackageReference Include="Microsoft.Extensions.Logging.Abstractions" />
  </ItemGroup>

  <!-- Every use case inherits the kernel's UseCase base; a global using makes it available in all files. -->
  <ItemGroup>
    <Using Include="ModularShop.Kernel.Application" />
  </ItemGroup>

</Project>
